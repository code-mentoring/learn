import * as React from 'react';
import { Button } from './Button';

export default {
  title: 'Buttons',
  parameters: {
    info: { inline: true }
  }
};

export const ButtonCell = () => (
  <Button>Test</Button>
);
