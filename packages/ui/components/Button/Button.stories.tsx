import React from 'react';

import { Button } from './Button';

export default {
  title: 'Button',
  parameters: {
    info: { inline: true }
  }
};

export const Default = () => <Button>Test</Button>;
