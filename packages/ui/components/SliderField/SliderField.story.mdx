import {SliderField} from './SliderField';
import {Card} from '../Card/Card';
import {Row} from '../../styleguide/Story';


# SliderField

## Default
By default, min = 0, max = 10, value = 0.
<Card>
    <Row>
      <SliderField  onChange = {v => console.log("The value is " + v)} />
    </Row>
</Card>

```tsx
<SliderField  onChange = {v => console.log("The value is " + v)} />
```

---

## Change default value
<Card>
    <Row>
      <SliderField min={1} max={8} value={5} onChange = {v => console.log("The value is " + v)}/>
    </Row>
</Card>

```tsx
<SliderField min={1} max={8} value={5} onChange = {v => console.log("The value is " + v)}/>
```
