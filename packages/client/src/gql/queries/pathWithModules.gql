query ($id: String!) {
  path(id: $id) {
    id
    name
    icon
    description
    createdAt
    progress
  }

  pathModules(pathId: $id) {
    id
    name
    icon
    type
    previous { id }
    completed
  }
}
