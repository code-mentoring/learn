query ($id: String!) {
  lesson(id: $id) {
    id
    name
    icon
    type
    previousId
    pathId
    completed
    lesson {
      storySections { content }
      questions {
        ... on QuestionMultiChoice { options code type }
      }
    }
  }
}
